# 像素画前置处理工具

## 痛点 / 产品定位

市面上已有像素画生成工具（如"画像素"），但从原图直接生成像素画时，结果容易变成**马赛克**而非真正的像素艺术。根本原因是：

> 照片的信息密度太高——数百万色、边缘模糊、细节冗杂。任何像素化工具直接处理都难以产出干净的像素画。

本工具定位为**像素画工具的前置处理器**，不直接生成最终像素图，而是将原图简化成"接近像素画风格的低信息密度图"，再交由"画像素"等工具完成最终像素化。

```
原图 → [本工具：前置简化] → 简化图 → [画像素等工具] → 像素画
```

与现有像素画工具的关系：**互补，而非竞争。**

---

## 项目概述

一个完全独立的 `pixel-art-tool.html` 网页工具。无需构建工具、无需服务器、无需依赖项——直接在浏览器中打开即可使用。

**输入：** 任意图片（照片、插画等）
**输出：** 简化后的 PNG 图片，适合导入像素画工具进行后续像素化

---

## 核心处理流程

```
上传图片
    │
    ▼
【阶段1】轮廓确认
  Sobel 边缘检测 → 阈值滑块 → 用户确认 / 手动描画修正
    │
    ▼
【阶段2】图像简化
  区域平滑（消除噪点/纹理） + 色调分离（posterization，减少色阶）
  + 可选风格化（Q版/卡通化：提升饱和度、简化阴影）
    │
    ▼
【阶段3】输出
  导出简化后的 PNG，供下游像素画工具使用
```

---

## 架构

**单文件：** `pixel-art-tool.html`
- `<style>` — 所有 CSS（深色主题）
- `<body>` — 语义化 HTML 布局（两栏：左侧控制面板，右侧预览区）
- `<script>` — 逻辑分层：
  - `EdgeDetector` 类 — Sobel 梯度边缘检测，阈值可调
  - `ImageSimplifier` 类 — 区域平滑 + 色调分离 + 风格化
  - UI 控制器 — 阶段状态管理、事件绑定、预览、下载

---

## 阶段1：轮廓确认

主体轮廓是简化处理的依据——平滑操作在轮廓内部进行，不跨越边界，避免主体与背景混融。

### 算法：Sobel 梯度
- 将图像转为灰度，分别应用 Sobel X 和 Sobel Y 卷积核
- 逐像素计算梯度幅值：`mag = sqrt(Gx² + Gy²)`
- 阈值判断：`mag > threshold` 的像素标记为边缘像素（二值掩码）
- 边缘图以与源图同尺寸的 `Uint8Array` 存储（1 = 边缘，0 = 非边缘）

### 阈值滑块
- 范围：0–255，默认值：50，实时重算（150ms 防抖）
- 预览画布以青色线条在原图上叠加显示当前检测到的边缘

### 用户确认
- "确认轮廓"按钮 → 进入阶段2
- "重新检测"按钮 → 退回阈值调整阶段
- "手动描画"按钮 → 进入手动描画模式：
  - **绘制模式**：鼠标左键拖拽 → 添加边缘像素（标记为1）
  - **擦除模式**：鼠标右键拖拽（或切换擦除按钮后左键）→ 清除边缘像素（标记为0）
  - 笔刷大小滑块：1–10px
  - 手动描画结果与阈值检测结果叠加写入同一 `Uint8Array` 边缘图
  - "完成描画"按钮 → 返回确认界面
- 确认后的边缘图（含手动修改）被冻结并传递给 `ImageSimplifier`

---

## 阶段2：图像简化

### 2-1 区域平滑
- 对非边缘区域执行双边滤波（Bilateral Filter）或简化版高斯模糊
- 边缘像素作为墙壁，平滑不跨越边界
- 目的：消除照片噪点、皮肤纹理、细小杂色，让色块更均匀

### 2-2 色调分离（Posterization）
- 将每个颜色通道的值量化到 N 个等级（默认 N=4，范围 2–8）
- 大幅减少色彩数量，使相邻像素颜色趋于一致
- 比完整的减色量化更轻量，结果更自然

### 2-3 风格化（4种预设，用户选择）

当前实现的4种风格：

| 风格 | 算法 | 特点 |
|------|------|------|
| 写实 | bilateral(r=2, σ=25) → RGB色阶(6级) | 最接近原图，轻度去噪 |
| 卡通 | bilateral(r=3, σ=50) → HSV色阶(H=12,S=4,V=6) → Sobel自适应暗化描边(阈值80, darken×0.35, 2px) | 平涂色块，边缘清晰 |
| 抽象 | gaussian(r=2) → pixelate(30px) → HSV色阶(H=8,S=3,V=4) | 几何块状，极简色彩 |
| 复古 | gaussian(r=2) → 暖色偏移(R×1.05, B×0.92) → HSV色阶(H=12,S=4,V=6) → 饱和度×0.70 | 暖色调，轮廓干净，褪色感 |

### 2-4 轮廓改变能力边界

**算法可做到（已实现或可实现）：**
- 消除细节轮廓噪点（发丝、皮肤纹理边缘）→ 双边滤波已覆盖
- 小区域色块合并，细节轮廓消失 → 色阶分离已覆盖

**算法无法做到（需要 AI）：**
- Q版化、比例改变、五官重新分布 → 需语义理解
- 背景/主体分离后分别处理 → 需语义分割
- 头发整块化 → 需语义区域识别

**后续扩展路线（待定）：** 在现有风格管线前增加可选「AI 卡通化」预处理（AnimeGANv2 / CartoonGAN，可本地运行），不依赖付费 API。

---

## 阶段3：输出

- 导出为 PNG（与原图相同分辨率，非像素图）
- 下载按钮：直接保存到本地
- 剪贴板复制按钮：方便直接粘贴到"画像素"等工具

---

## UI 布局

### 左侧面板——控制区

**阶段1 控件（确认轮廓前）：**
- 拖拽 / 点击上传区（支持 Ctrl+V 粘贴）
- **边缘阈值滑块** — 0–255，默认50
- 边缘叠加层开关
- "手动描画"按钮
- "确认轮廓"按钮

**手动描画模式控件（激活时替换上方控件）：**
- 绘制 / 擦除模式切换按钮
- **笔刷大小滑块** — 1–10px
- "完成描画"按钮

**阶段2 控件（确认轮廓后）：**
- **平滑强度滑块** — 0（关闭）至 10
- **色调分离级数滑块** — 2–8 级，默认 4
- **风格化开关** — Q版/卡通化（默认关闭）
- 实时预览（150ms 防抖）
- "重新调整轮廓"按钮 → 返回阶段1

**输出区：**
- 下载简化图按钮（PNG）
- 复制到剪贴板按钮

### 右侧面板——预览区

**阶段1 预览：**
- 原图 + 青色边缘叠加层，实时更新

**阶段2 预览：**
- 原图 / 简化图并排或分屏对比（可拖拽分隔线）
- 缩放控制（1x、2x、4x）

---

## 技术选型

| 技术 | 用途 |
|------|------|
| Canvas 2D API | 所有像素操作（`getImageData`、`putImageData`、`drawImage`） |
| Sobel 算子（内联） | 边缘检测 |
| 双边滤波 / 高斯模糊（内联） | 区域平滑，保边去噪 |
| 色调分离（内联） | 通道值量化，减少色彩层次 |
| 纯 HTML/CSS/JS | 无框架、无构建工具、完全离线 |

---

## 实现阶段

| 阶段 | 任务 |
|------|------|
| 1 | HTML 骨架 + CSS 两栏布局 + 阶段状态机 |
| 2 | 图片加载 + 阶段1：Sobel 边缘检测 + 阈值滑块 + 边缘叠加预览 |
| 3 | 阶段1 三种操作：阈值调整 / 手动描画模式（绘制+擦除+笔刷大小）/ 确认；边缘图存储 |
| 4 | 阶段2：区域平滑（双边滤波，边缘为墙壁） |
| 5 | 阶段2：色调分离（通道量化） |
| 6 | 阶段2：风格化选项（Q版/卡通化） |
| 7 | 阶段2 实时预览 + 分屏对比 |
| 8 | 阶段3：下载 PNG + 复制到剪贴板 |
| 9 | 实时预览防抖、加载动画、边界情况处理 |

---

## 关键实现说明

- **双边滤波优于纯高斯模糊：** 双边滤波在平滑时保留边缘，避免轮廓被模糊掉。实现复杂度适中，可内联约80行代码完成。
- **色调分离而非完整量化：** 色调分离（通道等距量化）比 Median Cut 更轻量，结果更平滑自然，更适合作为"预处理"而非最终输出。
- **边缘图作为平滑掩码：** 阶段1确认的边缘图在阶段2中用作平滑操作的边界约束，确保主体与背景不互相渗色。
- **输出为原始分辨率 PNG：** 本工具不做像素化，不降采样，保持原始尺寸输出——像素化由下游工具完成。
